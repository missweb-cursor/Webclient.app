# Allow build scripts for these trusted packages
enable-pre-post-scripts=true

# Approve build scripts for specific packages
# These are legitimate dependencies that need to run build scripts
# @parcel/watcher - file watching library used by Next.js
# sharp - image processing library
# unrs-resolver - TypeScript resolver
